import 'package:flutter/material.dart';

abstract class AppTheme {
  // Light Theme
  static ThemeData lightMode = ThemeData(
    appBarTheme: AppBarTheme(backgroundColor: Color(0xff5372F0))
      );

}
